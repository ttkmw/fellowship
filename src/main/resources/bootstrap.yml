spring:
  application:
    name: chatservice # 애플리케이션 이름은 컨피그 서버의 디렉터리 이름과 일치해야 한다.
  profiles:
    active:
      alpha
  cloud:
    config:
      uri: http://localhost:8888
      label: develop
encrypt:
  key: IMSTMMETRIC
