language: java
jdk:
  - openjdk8
branches:
  only:
    - develop
sudo: required
services:
  - docker
notifications:
  email:
    - wom2277@naver.com
    on_failure: always

before_install:
  - gem install travis -v 1.10.0 --no-document
  - sudo curl -k -o /usr/local/bin/ecs-cli https://s3.amazonaws.com/amazon-ecs-cli/ecs-cli-linux-amd64-latest
  - sudo chmod +x /usr/local/bin/ecs-cli
  - export BUILD_NAME=chat-develop-$(date -u "+%Y%m%d%H%M%S")-0.0.1
  - export CONTAINER_IP=54.180.121.66

script:
  - sh travis_scripts/tag_build.sh
